type Star @model {    
    id: ID!   @globalId
    paidAt:   Date! @rename(attribute: "paid_at")
    event:    Event! @belongsTo(relation: "parent")
    user:     User! @belongsTo(relation: "parent")   
}
 
